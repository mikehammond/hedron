query SearchProducts(
    $query: String
  ) {
    searchProducts(
      searchQueryInput: {
        query: $query
      }
    ) {
      name
      summary
      description
      featured {
        url
      }
    }
  }
