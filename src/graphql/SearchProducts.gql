query SearchProducts(
    $query: String
  ) {
    searchProducts(
      searchQueryInput: {
        query: $query
      }
    ) {
      name
      slug
      summary
      description
      featured {
        url
      }
    }
  }
